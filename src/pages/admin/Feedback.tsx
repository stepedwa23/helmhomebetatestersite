import PagePlaceholder from '../../components/PagePlaceholder'

export default function AdminFeedback() {
  return (
    <PagePlaceholder
      title="Feedback"
      description="Per-cycle qualitative feedback from testers (rating + comments)."
    />
  )
}
