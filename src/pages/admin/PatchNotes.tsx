import PagePlaceholder from '../../components/PagePlaceholder'

export default function AdminPatchNotes() {
  return (
    <PagePlaceholder
      title="Patch notes"
      description="Manage app versions and rich-text patch notes (TipTap). Mark one version as current — that's what the tester dashboard shows."
    />
  )
}
