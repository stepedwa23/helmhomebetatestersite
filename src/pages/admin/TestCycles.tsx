import PagePlaceholder from '../../components/PagePlaceholder'

export default function AdminTestCycles() {
  return (
    <PagePlaceholder
      title="Test cycles"
      description="Rounds of testing: name, build version, start/end date, assigned testers, status."
    />
  )
}
