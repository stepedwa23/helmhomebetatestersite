import PagePlaceholder from '../../components/PagePlaceholder'

export default function AdminSettings() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Project metadata, notification preferences, account."
    />
  )
}
