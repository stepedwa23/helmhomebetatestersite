import PagePlaceholder from '../../components/PagePlaceholder'

export default function AdminSuggestions() {
  return (
    <PagePlaceholder
      title="Suggestions (admin)"
      description="Triage tester-submitted feature ideas: set status (new / under review / planned / declined / shipped) and add admin notes."
    />
  )
}
