import PagePlaceholder from '../../components/PagePlaceholder'

export default function Suggestions() {
  return (
    <PagePlaceholder
      title="Suggestions"
      description="Submit a new feature idea, or browse what other testers have suggested and its current status."
    />
  )
}
