import PagePlaceholder from '../../components/PagePlaceholder'

export default function ReportBug() {
  return (
    <PagePlaceholder
      title="Report a bug"
      description="Title, description, steps to reproduce, severity, category, OS, calm-mode state, screenshot."
    />
  )
}
